# ── Phase 0-2 AI Engine (Ollama / local LLM + RAG) ───────────────────────────
requests>=2.31.0          # HTTP client for Ollama API calls
pydantic>=2.0.0           # Schema validation for LLM output

# ── Phase 2: RAG / Vector Store ───────────────────────────────────────────────
chromadb>=1.5.0           # Vector database (already installed; bundles all-MiniLM-L6-v2 ONNX)
# Note: sentence-transformers is NOT required separately.
# ChromaDB's DefaultEmbeddingFunction uses the bundled ONNX model automatically.

# ── FastAPI server (existing, unchanged) ──────────────────────────────────────
fastapi
uvicorn
python-dotenv